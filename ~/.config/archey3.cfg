[core]
display_modules = distro(), uname(n), uname(r), uptime(), wm(), packages(), ram(), env(editor), env(shell), env(browser), fs(/), fs(/mnt/Others), fs(/home/cache), mpd(songs), sensor("coretemp-isa-0000")
align = top
color = blue
