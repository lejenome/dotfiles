[core]
display_modules = distro(), uname(n), uname(r), uptime(), wm(), de(), packages(), ram(), env(editor), env(shell), fs(/), fs(/mnt/Others), mpd(songs), sensor("coretemp-isa-0000")
align = top
color = blue

